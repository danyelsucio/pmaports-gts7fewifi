name: build
on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip git openssl device-tree-compiler bmap-tools
          
      - name: Install pmbootstrap from source
        run: |
          git clone https://gitlab.postmarketos.org/postmarketOS/pmbootstrap.git
          pip3 install --user ./pmbootstrap
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          
      - name: Initialize pmbootstrap config
        env:
          PMBOOTSTRAP_WORK: /tmp/pmbootstrap_work
          PMBOOTSTRAP_DEVICE: gts7fewifi
          PMBOOTSTRAP_CHANNEL: edge
          PMBOOTSTRAP_NO_FDE: "yes"
          PMBOOTSTRAP_APORTS: ${{ github.workspace }}
        run: |
          mkdir -p /tmp/pmbootstrap_work
          cd ${{ github.workspace }}
          
          # Force pmbootstrap to use the current directory as the aports folder directly
          pmbootstrap --aports . init
          
      - name: Build rootfs for gts7fewifi
        run: |
          pmbootstrap install --no-fde
          pmbootstrap export
          
          mkdir -p /tmp/out
          cp -rv /tmp/postmarketOS-export/* /tmp/out/ || true
          
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: pmos-gts7fewifi-image
          path: /tmp/out/
